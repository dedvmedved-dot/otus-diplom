apiVersion: apps/v1
kind: Deployment
metadata:
  name: piraeus-operator-gencert
  namespace: piraeus-datastore
spec:
  template:
    spec:
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
