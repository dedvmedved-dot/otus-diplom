# P5C2 — Validation PVC (temporary, 1Gi RWO, piraeus-r2)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: p5c2-r2-test
  namespace: p5c2-validation
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: piraeus-r2
  resources:
    requests:
      storage: 1Gi
