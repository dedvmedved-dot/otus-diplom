# secret-check.txt — TASK-002 pre-commit secret scan
# captured: TASK-002 P0 repository bootstrap
# method: manual pattern scan over `git diff --cached` (no scanner installed;
#         none installed solely for this task, per §21). Read-only.

SCAN SCOPE: all staged files for the initial commit.

PATTERNS CHECKED (case-insensitive):
  ghp_/gho_/ghs_/github_pat_  (GitHub tokens)
  AKIA[0-9A-Z]{16}            (AWS access key id)
  xox[baprs]-                 (Slack tokens)
  -----BEGIN ... PRIVATE KEY  (RSA/OPENSSH/EC/DSA/PGP private keys)
  Authorization: Bearer       (bearer tokens)
  eyJ...                       (JWT-like)
  password / secret / token / api_key / credentials (literal terms)

RESULT: PASS — no real credential values found.

The only literal-term matches are PROHIBITION TEXT (not secrets):
  - docs/governance/project-governance.md: "...SSH private keys, BMC/S3/registry
    credentials..." (secret-policy prose)
  - inventory/hosts.yaml.example: "# NO passwords, NO private keys, NO BMC
    passwords, NO real secrets here." (template safety comment)

Real GitHub PAT value pattern (ghp_ + 36 chars): NONE FOUND in staged content.
Remote URL: https://github.com/dedvmedved-dot/otus-diplom.git — NO embedded token.
.git/config: NO token present.

git diff --cached --check (whitespace/conflict markers): OK.

No secrets printed into this evidence file.
