# P6D-R2 — temporary validation Gateway (HTTP only)
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: p6d-r2-gateway
  namespace: p6d-r2-validation
spec:
  gatewayClassName: eg
  listeners:
    - name: http
      protocol: HTTP
      port: 80
