# TASK-015-R3 — R2 Connector Defects

D1 — corrected compile path никогда не исполнялся: все R2 evidence = CACHE_HIT,
     «skipping rebuild». R3 требует forced fresh compile (CACHE_HIT=0).
D2 — health PHASE 8 резолвит по serial-only; должен быть composite + unique.
D3 — safety checks fail open: rc запросов не enforced, ошибка команды → пустое
     значение → интерпретируется как 0/безопасно. Должно быть FAIL.
D4 — fingerprint не валидирует rc/точное число байт dd (нужно exact 1 MiB).
D5 — compile helper не верифицирует эффективные ro-флаги mount'ов.
D6 — R2 содержит failed node-01 health (cannot bind /dev, rc=3) в том же RUN_ID.
D7 — P1-P4 postcheck не является полноценным in-playbook Gate.
D8 — K8s storage-state query fail-open (|| true → ошибки API → 0 объектов).
