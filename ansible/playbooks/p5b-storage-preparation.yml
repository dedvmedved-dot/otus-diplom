---
# p5b-storage-preparation.yml
# ═══════════════════════════════════════════════════════════════════════════
# TOMBSTONE — TASK-016-R2
#
# The historical TASK-016 destructive LVM thin storage implementation has been
# REPLACED by this fail-closed tombstone. The original destructive code is NOT
# retained as reusable code.
#
# TASK-016 authorization: CONSUMED_CLOSED, non-reusable.
#   architect_authorized: false
#   consumed: true
#   reusable: false
#   state: CONSUMED_CLOSED
#   consumed_by_commit: efdd3b1831208ccee810d392bbb3ce690b2104b5
#
# This playbook contains:
#   - NO executable pvcreate
#   - NO executable vgcreate
#   - NO executable lvcreate
#   - NO destructive branch
#   - NO device access
#   - NO storage command
#
# Every invocation fails with AUTHORIZATION_CONSUMED before any device access.
#
# Any future destructive storage action requires a NEW Architect task, a NEW
# authorization object, and a NEW playbook.
# ═══════════════════════════════════════════════════════════════════════════

- name: "P5B storage preparation (TOMBSTONED — authorization consumed)"
  hosts: all
  gather_facts: false
  become: false
  tasks:
    - name: TOMBSTONE — authorization consumed (fail-closed, no device access)
      ansible.builtin.fail:
        msg: >-
          AUTHORIZATION_CONSUMED: TASK-016 destructive storage authorization is
          CONSUMED_CLOSED and non-reusable. This playbook is tombstoned and
          contains no destructive code. A NEW Architect task, NEW authorization
          object and NEW playbook are required for any future destructive
          storage action. No device was accessed, no storage command was run.
